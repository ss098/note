language: node_js

node_js:
  - "node"

services:
  - mongodb

install:
  - npm install

script:
  - npm start
  
env:
  - BASEURL=https://www.example.com
  - MONGO_CONNECT_STRING=mongodb://127.0.0.1/note